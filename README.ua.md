# 📝 NoteHub — Додаток для нотаток з автентифікацією

Це навчальний проект на Next.js 15 з повноцінною системою автентифікації та управління нотатками.

## 🎯 Що це за проект?

**NoteHub** — це веб-додаток, де користувачі можуть:

- ✅ Реєструватися та входити в систему
- ✅ Створювати, переглядати та видаляти нотатки
- ✅ Фільтрувати нотатки за тегами
- ✅ Шукати нотатки
- ✅ Редагувати свій профіль

## 🛠️ Технології, які використовуються

### Основні технології:

- **Next.js 15** — React фреймворк для створення сучасних веб-додатків
- **TypeScript** — типізована версія JavaScript для безпечнішого коду
- **React 19** — бібліотека для створення інтерфейсів

### Бібліотеки для управління станом:

- **Zustand** — проста бібліотека для глобального стану (альтернатива Redux)
- **TanStack Query (React Query)** — управління серверним станом, кешування, mutations

### Форми та валідація:

- **Formik** — бібліотека для роботи з формами
- **Yup** — валідація даних форм

### HTTP запити:

- **Axios** — бібліотека для відправки HTTP запитів

### Інші корисні інструменти:

- **use-debounce** — затримка виконання функцій (для автозбереження)
- **react-paginate** — пагінація для списків
- **cookie** — робота з cookies

## 📁 Структура проекту

```
09-auth/
├── app/                          # Next.js App Router
│   ├── (auth routes)/           # Публічні сторінки (sign-in, sign-up)
│   ├── (private routes)/        # Захищені сторінки (profile, notes)
│   ├── @modal/                  # Паралельні маршрути (модалки)
│   ├── api/                     # API Routes (бекенд)
│   ├── layout.tsx               # Кореневий layout
│   └── page.tsx                 # Головна сторінка
│
├── components/                   # React компоненти
│   ├── AuthProvider/            # Перевірка авторизації
│   ├── Header/                  # Шапка сайту
│   ├── Footer/                  # Підвал сайту
│   ├── NoteForm/                # Форма створення нотатки
│   ├── NoteList/                # Список нотаток
│   └── ...
│
├── lib/                         # Утиліти та бізнес-логіка
│   ├── api/                     # API функції
│   │   ├── clientApi.ts        # Клієнтські запити
│   │   └── serverApi.ts        # Серверні запити
│   └── store/                   # Zustand stores
│       ├── authStore.ts        # Стан авторизації
│       └── noteStore.ts        # Стан нотаток
│
├── types/                       # TypeScript типи
│   ├── user.ts                 # Типи користувача
│   └── note.ts                 # Типи нотаток
│
└── middleware.ts                # Next.js Middleware (захист маршрутів)
```

## 🔐 Як працює автентифікація?

### 1. **Токени (Ключі доступу)**

Коли ви входите в систему, сервер видає вам два "ключі":

- **accessToken** (Короткий ключ) — живе 10-15 хвилин
  - Використовується для звичайних запитів
  - Якщо хтось його викраде, він швидко стане недійсним

- **refreshToken** (Довгий ключ) — живе 7-30 днів
  - Використовується для оновлення accessToken
  - Зберігається безпечніше

### 2. **Middleware (Охоронець сторінок)**

Файл `middleware.ts` — це як охоронець перед входом у будівлю.

**Що він робить?**

1. Перевіряє, чи ви маєте токени
2. Якщо accessToken застарів, отримує новий через refreshToken
3. Не пускає на приватні сторінки без авторизації
4. Перенаправляє залогінених користувачів з sign-in на profile

### 3. **AuthProvider (Перевірка при запуску)**

Компонент `AuthProvider` обгортає весь додаток і:

- Перевіряє, чи ви залогінені при завантаженні сторінки
- Завантажує ваші дані користувача
- Показує loader під час перевірки
- Автоматично розлогінює, якщо сесія недійсна

## 🎨 Ключові концепції для новачків

### 1. **Що таке стан (State)?**

Стан — це дані, які можуть змінюватися. Коли стан змінюється, React перемальовує компонент.

**Локальний стан (useState):**

```typescript
const [count, setCount] = useState(0); // count = 0
setCount(5); // тепер count = 5, компонент перемальовується
```

**Глобальний стан (Zustand):**

```typescript
// Стан, доступний у всіх компонентах
const user = useAuthStore((state) => state.user);
```

### 2. **Що таке хуки React?**

Хуки — це спеціальні функції, які додають можливості компонентам.

**useState** — зберігає дані, які можуть змінюватися

```typescript
const [name, setName] = useState('Іван');
```

**useEffect** — виконує код після рендеру (наприклад, завантаження даних)

```typescript
useEffect(() => {
  console.log('Компонент завантажився!');
}, []); // [] = виконати один раз
```

**useCallback** — запам'ятовує функцію (оптимізація)

```typescript
const handleClick = useCallback(() => {
  console.log('Клік!');
}, []); // функція не створюється заново при кожному рендері
```

**useMemo** — запам'ятовує результат обчислення

```typescript
const total = useMemo(() => {
  return items.reduce((sum, item) => sum + item.price, 0);
}, [items]); // перераховується тільки коли items змінюється
```

### 3. **Що таке Debounce?**

Debounce — це затримка виконання функції до моменту, коли користувач перестане діяти.

**Без debounce:**

```
Користувач друкує: H e l l o
Запити на сервер:    5 запитів (по одному на кожну літеру)
```

**З debounce (1 секунда):**

```
Користувач друкує: H e l l o
Чекаємо 1 секунду після останньої літери
Запит на сервер:   1 запит (Hello)
```

**Приклад:**

```typescript
const debouncedSave = useDebouncedCallback((text) => {
  saveToServer(text);
}, 1000); // Зачекати 1 секунду

// Користувач друкує швидко
onChange((e) => debouncedSave(e.target.value));
// Функція викличеться один раз через 1 сек після зупинки друку
```

### 4. **Що таке React Query (TanStack Query)?**

React Query — це бібліотека для роботи з даними з сервера.

**Навіщо потрібна?**

- ✅ Автоматично керує loading/error станами
- ✅ Кешує дані (не завантажує знову, якщо вже є)
- ✅ Автоматично оновлює дані у фоні
- ✅ Повторює запити при помилках

**Приклад:**

```typescript
// Mutation — для зміни даних (POST, PUT, DELETE)
const { mutate, isPending } = useMutation({
  mutationFn: createNote, // Функція для відправки
  onSuccess: () => {
    console.log('Успіх!');
  },
});

// Відправити дані
mutate({ title: 'Моя нотатка' });
```

### 5. **Що таке TypeScript типи?**

TypeScript додає типи до JavaScript, щоб зловити помилки до запуску коду.

**Приклад:**

```typescript
// JavaScript (помилку побачимо тільки при запуску)
function greet(name) {
  return 'Hello, ' + name.toUpperCase();
}
greet(123); // Помилка! 123 не має методу toUpperCase()

// TypeScript (помилку побачимо одразу в редакторі)
function greet(name: string) {
  return 'Hello, ' + name.toUpperCase();
}
greet(123); // ❌ TypeScript покаже помилку ТУТ
```

## 🚀 Як запустити проект?

### 1. Встановити залежності:

```bash
npm install
```

### 2. Створити файл `.env.local` та додати:

```
NEXT_PUBLIC_API_URL=http://localhost:3000
```

### 3. Запустити сервер розробки:

```bash
npm run dev
```

### 4. Відкрити в браузері:

```
http://localhost:3000
```

## 📚 Корисні ресурси для навчання

### Next.js:

- [Офіційна документація Next.js](https://nextjs.org/docs)
- [Next.js для початківців (відео)](https://www.youtube.com/watch?v=Sklc_fQBmcs)

### React:

- [Офіційна документація React](https://react.dev)
- [React хуки](https://react.dev/reference/react)

### TypeScript:

- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)
- [TypeScript для початківців](https://www.typescriptlang.org/docs/handbook/typescript-from-scratch.html)

### Zustand:

- [Документація Zustand](https://docs.pmnd.rs/zustand/getting-started/introduction)

### React Query:

- [Документація TanStack Query](https://tanstack.com/query/latest/docs/framework/react/overview)

## 💡 Поради для новачків

### 1. **Не намагайтеся вивчити все одразу**

Зосередьтеся на одному файлі чи концепції за раз.

### 2. **Експериментуйте**

Змініть щось у коді та подивіться, що станеться. Не бійтеся зламати!

### 3. **Читайте помилки**

Помилки в консолі браузера та терміналі — ваші друзі. Вони підказують, що не так.

### 4. **Використовуйте console.log()**

Додавайте `console.log()` щоб побачити, які дані є в змінній:

```typescript
console.log('formValues:', formValues);
```

### 5. **Встановіть React DevTools**

Розширення для браузера, яке показує структуру React компонентів:

- [Chrome](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi)
- [Firefox](https://addons.mozilla.org/en-US/firefox/addon/react-devtools/)

## 🐛 Типові помилки та їх вирішення

### Помилка: "Module not found"

**Рішення:** Перевірте, чи встановлені всі залежності:

```bash
npm install
```

### Помилка: "Hydration failed"

**Рішення:** Це означає, що HTML від сервера не збігається з клієнтом. Перевірте:

- Чи не використовуєте `localStorage` без перевірки `typeof window !== 'undefined'`
- Чи не використовуєте `Math.random()` або `Date.now()` у render

### Помилка: "Cannot read property of undefined"

**Рішення:** Використовуйте optional chaining:

```typescript
// ❌ Погано
const name = user.profile.name;

// ✅ Добре
const name = user?.profile?.name;
```

## 📝 Ліцензія

Цей проект створений для навчальних цілей.

---

**Успіхів у навчанні! 🚀**

Якщо у вас виникли питання, читайте коментарі у коді — там детально пояснено, як все працює.
